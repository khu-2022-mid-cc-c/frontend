import React from "react";

function fileInfo(props) {
    // console.log(props.info)
    return (
        <div id="fileInfo">
            <p>{props.info}</p>
        </div>
    );
}

export default fileInfo;